#!/usr/bin/env ruby -KU

require 'rubygems'
require 'pathname'

$LOAD_PATH.unshift(Pathname.new(__FILE__).dirname.parent)

require 'service'

Alfred::Service.run! :host => Config['service']['host'], :port => Config['service']['port']
